** FORMIK  , its a npm package that will help a lot in making forms in REACT.

Single Page Applications : once the whole page is loaded, it will not be loaded again when we are clicking on something or changing the data, these are components that base architecture, on clikcing component should be re render.

Routing :   There a two types of routing.
            1. Client side routing : In react, we do client side routing.
            2. Server side routing : all are the pages comes from the server.

            For Routing , in this project we are using "react-router-dom"
            in App component it is used just checkout.....

